import { FC } from "react";
import { Testimonial } from "Types";
import { SliderWrapper } from "./Styles"

const SliderSection: FC<{ testimonials?: Testimonial[] }> = ({ testimonials }) => {
  return (
    <SliderWrapper>

    </SliderWrapper>
  );
};

export default SliderSection;
