export type PageMeta = {
  id: number,
  scope: string,
  title: string,
  description: string,
  abstract: string,
  keywords: string,
};

export type PageOgMeta = {
  id: number,
  scope: string,
  page_url: string,
  title: string,
  desc: string,
  image_url: string,
};